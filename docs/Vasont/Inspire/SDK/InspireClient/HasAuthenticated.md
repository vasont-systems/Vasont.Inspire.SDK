# HasAuthenticated property (Vasont.Inspire.SDK.InspireClient)
Gets a value indicating whether the client has authenticated with the server at some point

## Signature
```csharp
public bool HasAuthenticated
{
    get;
}
```
