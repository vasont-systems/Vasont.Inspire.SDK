# RoutePrefix property (Vasont.Inspire.SDK.InspireClientConfiguration)
Gets or sets an optional URI route prefix to add to all requests.

## Signature
```csharp
public string RoutePrefix
{
    get;
    set;
}
```
